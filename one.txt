FIRST PAGE 


<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>ParkEzy</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700;900&family=Rajdhani:wght@400;500;600&display=swap" rel="stylesheet">
<style>
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

:root {
  --bg: #0a0e1a;
  --bg2: #0f1525;
  --card: rgba(15,21,40,0.92);
  --border: rgba(0,210,220,0.18);
  --cyan: #00d4dc;
  --yellow: #f5c842;
  --orange: #ff6b35;
  --orange2: #ff4500;
  --text: #e8eaf0;
  --muted: #7a8499;
  --input-bg: rgba(255,255,255,0.05);
  --input-border: rgba(0,210,220,0.25);
}

body {
  font-family: 'Rajdhani', sans-serif;
  background: var(--bg);
  min-height: 100vh;
  overflow-x: hidden;
  color: var(--text);
}

/* ─── STARS ─── */
.stars {
  position: fixed; inset: 0; pointer-events: none; z-index: 0;
  background:
    radial-gradient(1px 1px at 10% 15%, rgba(255,255,255,0.6) 0%, transparent 100%),
    radial-gradient(1px 1px at 25% 40%, rgba(255,255,255,0.4) 0%, transparent 100%),
    radial-gradient(1px 1px at 40% 10%, rgba(255,255,255,0.5) 0%, transparent 100%),
    radial-gradient(1px 1px at 60% 30%, rgba(255,255,255,0.3) 0%, transparent 100%),
    radial-gradient(1px 1px at 75% 60%, rgba(255,255,255,0.6) 0%, transparent 100%),
    radial-gradient(1px 1px at 85% 20%, rgba(255,255,255,0.4) 0%, transparent 100%),
    radial-gradient(1px 1px at 90% 75%, rgba(255,255,255,0.5) 0%, transparent 100%),
    radial-gradient(1px 1px at 15% 80%, rgba(255,255,255,0.3) 0%, transparent 100%),
    radial-gradient(1px 1px at 50% 55%, rgba(255,255,255,0.4) 0%, transparent 100%),
    radial-gradient(1px 1px at 33% 70%, rgba(255,255,255,0.5) 0%, transparent 100%),
    radial-gradient(1px 1px at 68% 88%, rgba(255,255,255,0.3) 0%, transparent 100%),
    radial-gradient(1px 1px at 5% 50%, rgba(255,255,255,0.5) 0%, transparent 100%),
    radial-gradient(1px 1px at 95% 45%, rgba(255,255,255,0.4) 0%, transparent 100%);
}

/* ─── ROAD ─── */
.road {
  position: fixed;
  bottom: 0; left: 0; right: 0;
  height: 90px;
  background: linear-gradient(180deg, #1a1a2e 0%, #111120 100%);
  z-index: 1;
  border-top: 2px solid rgba(255,255,255,0.06);
  overflow: hidden;
}
.road::after {
  content: '';
  position: absolute;
  top: 50%; left: -100%;
  width: 300%;
  height: 4px;
  background: repeating-linear-gradient(90deg,
    var(--yellow) 0px, var(--yellow) 40px,
    transparent 40px, transparent 80px
  );
  animation: dashMove 1.2s linear infinite;
}
@keyframes dashMove {
  from { transform: translateX(0); }
  to   { transform: translateX(80px); }
}

/* ─── CAR ─── */
.car-wrap {
  position: fixed;
  bottom: 14px; left: -160px;
  z-index: 2;
  animation: carDrive 8s linear infinite;
  filter: drop-shadow(0 0 18px rgba(255,100,30,0.55));
}
@keyframes carDrive {
  0%   { left: -160px; }
  100% { left: calc(100vw + 20px); }
}
.car {
  width: 140px;
  position: relative;
}
.car-body {
  width: 140px; height: 48px;
  background: linear-gradient(135deg, #cc3300 0%, #ff4500 50%, #cc3300 100%);
  border-radius: 10px 28px 10px 10px;
  position: relative;
  box-shadow: 0 0 20px rgba(255,80,0,0.5);
}
.car-body::before {
  content: '';
  position: absolute;
  top: -22px; left: 30px;
  width: 72px; height: 30px;
  background: linear-gradient(135deg, #cc3300, #ff5500);
  border-radius: 8px 20px 0 0;
  box-shadow: inset 0 6px 12px rgba(0,0,0,0.3);
}
.car-windshield {
  position: absolute;
  top: -18px; left: 38px;
  width: 56px; height: 24px;
  background: rgba(100,200,255,0.15);
  border: 1px solid rgba(100,200,255,0.3);
  border-radius: 6px 16px 0 0;
}
.car-stripe {
  position: absolute;
  top: 16px; left: 10px; right: 20px; height: 6px;
  background: rgba(255,255,255,0.12);
  border-radius: 3px;
}
.car-badge {
  position: absolute;
  top: 8px; left: 52px;
  width: 24px; height: 18px;
  background: rgba(255,255,255,0.15);
  border: 1px solid rgba(255,255,255,0.25);
  border-radius: 3px;
  display: flex; align-items: center; justify-content: center;
  font-size: 8px; font-weight: 700; color: rgba(255,255,255,0.7);
  font-family: 'Orbitron', monospace;
}
.wheel {
  position: absolute; bottom: -11px;
  width: 26px; height: 26px;
  background: radial-gradient(circle at 50% 50%, #555 30%, #222 100%);
  border-radius: 50%;
  border: 3px solid #888;
  box-shadow: 0 0 8px rgba(0,0,0,0.5);
}
.wheel::after {
  content: '';
  position: absolute;
  top: 50%; left: 50%;
  transform: translate(-50%,-50%);
  width: 8px; height: 8px;
  background: #ccc;
  border-radius: 50%;
}
.wheel-front { right: 12px; }
.wheel-back  { left: 12px; }
.headlight {
  position: absolute;
  right: -8px; top: 14px;
  width: 18px; height: 12px;
  background: rgba(255,240,180,0.9);
  border-radius: 2px;
  box-shadow: 0 0 20px 8px rgba(255,220,100,0.4), 0 0 60px 20px rgba(255,200,50,0.15);
}
.taillight {
  position: absolute;
  left: -4px; top: 14px;
  width: 8px; height: 10px;
  background: rgba(255,60,60,0.8);
  border-radius: 2px;
  box-shadow: 0 0 10px 4px rgba(255,0,0,0.4);
}

/* ─── PAGES ─── */
.page {
  position: relative; z-index: 10;
  min-height: 100vh;
  display: none;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 2rem 1rem 100px;
}
.page.active { display: flex; }

/* ─── LOGO ─── */
.logo {
  font-family: 'Orbitron', monospace;
  font-size: clamp(28px, 5vw, 42px);
  font-weight: 900;
  letter-spacing: 2px;
  margin-bottom: 6px;
}
.logo span.p { color: var(--cyan); }
.logo span.e { color: var(--yellow); }
.tagline {
  font-family: 'Rajdhani', sans-serif;
  font-size: 12px;
  letter-spacing: 4px;
  color: var(--muted);
  text-transform: uppercase;
  margin-bottom: 3rem;
}

/* ─── ROLE CARDS ─── */
.role-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
  width: 100%;
  max-width: 680px;
}
.role-card {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 2.5rem 2rem;
  cursor: pointer;
  transition: transform 0.3s, border-color 0.3s, box-shadow 0.3s;
  text-align: center;
  position: relative;
  overflow: hidden;
  backdrop-filter: blur(12px);
}
.role-card::before {
  content: '';
  position: absolute;
  inset: 0;
  opacity: 0;
  transition: opacity 0.3s;
  border-radius: 16px;
}
.role-card.user::before  { background: radial-gradient(circle at 50% 0%, rgba(0,212,220,0.12) 0%, transparent 70%); }
.role-card.host::before  { background: radial-gradient(circle at 50% 0%, rgba(255,107,53,0.12) 0%, transparent 70%); }
.role-card:hover::before { opacity: 1; }
.role-card:hover {
  transform: translateY(-6px);
  box-shadow: 0 20px 50px rgba(0,0,0,0.5);
}
.role-card.user:hover  { border-color: var(--cyan); box-shadow: 0 20px 50px rgba(0,212,220,0.2); }
.role-card.host:hover  { border-color: var(--orange); box-shadow: 0 20px 50px rgba(255,107,53,0.2); }

.role-icon {
  font-size: 52px;
  margin-bottom: 1rem;
  display: block;
  filter: drop-shadow(0 0 12px rgba(255,255,255,0.1));
}
.role-title {
  font-family: 'Orbitron', monospace;
  font-size: 20px;
  font-weight: 700;
  margin-bottom: 8px;
}
.role-card.user  .role-title { color: var(--cyan); }
.role-card.host  .role-title { color: var(--orange); }
.role-desc {
  font-size: 14px;
  color: var(--muted);
  margin-bottom: 1.5rem;
  line-height: 1.5;
}
.role-btn {
  display: inline-block;
  padding: 10px 28px;
  border: none;
  border-radius: 30px;
  font-family: 'Orbitron', monospace;
  font-size: 12px;
  font-weight: 700;
  letter-spacing: 1px;
  cursor: pointer;
  transition: transform 0.2s, box-shadow 0.2s;
}
.role-card.user  .role-btn { background: linear-gradient(135deg, #00b8c4, #0088aa); color: #fff; }
.role-card.host  .role-btn { background: linear-gradient(135deg, var(--orange), var(--orange2)); color: #fff; }
.role-btn:hover { transform: scale(1.06); box-shadow: 0 6px 20px rgba(0,0,0,0.4); }

/* ─── AUTH CARD ─── */
.auth-card {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 20px;
  padding: 2rem 2.5rem 2.5rem;
  width: 100%;
  max-width: 420px;
  backdrop-filter: blur(16px);
  box-shadow: 0 30px 80px rgba(0,0,0,0.5), inset 0 1px 0 rgba(255,255,255,0.06);
}

/* Tab switcher */
.tab-row {
  display: flex;
  background: rgba(255,255,255,0.05);
  border-radius: 30px;
  padding: 4px;
  margin-bottom: 2rem;
}
.tab {
  flex: 1;
  padding: 10px;
  border: none;
  border-radius: 26px;
  font-family: 'Rajdhani', sans-serif;
  font-size: 15px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s;
  background: transparent;
  color: var(--muted);
  letter-spacing: 0.5px;
}
.tab.active {
  background: linear-gradient(135deg, var(--cyan), #0099aa);
  color: #fff;
  box-shadow: 0 4px 15px rgba(0,180,200,0.35);
}

/* form fields */
.field { margin-bottom: 1.2rem; }
.field label {
  display: block;
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--muted);
  margin-bottom: 8px;
}
.field input {
  width: 100%;
  padding: 13px 16px;
  background: var(--input-bg);
  border: 1px solid var(--input-border);
  border-radius: 10px;
  color: var(--text);
  font-family: 'Rajdhani', sans-serif;
  font-size: 15px;
  outline: none;
  transition: border-color 0.2s, box-shadow 0.2s;
}
.field input::placeholder { color: rgba(122,132,153,0.6); }
.field input:focus {
  border-color: var(--cyan);
  box-shadow: 0 0 0 3px rgba(0,212,220,0.12);
}

/* remember / forgot */
.meta-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 1.5rem;
}
.remember {
  display: flex; align-items: center; gap: 8px;
  font-size: 13px; color: var(--muted); cursor: pointer;
}
.remember input[type=checkbox] {
  width: 16px; height: 16px;
  accent-color: var(--cyan);
  cursor: pointer;
}
.forgot {
  font-size: 13px;
  color: var(--cyan);
  text-decoration: none;
  transition: opacity 0.2s;
}
.forgot:hover { opacity: 0.7; }

/* CTA button */
.cta-btn {
  width: 100%;
  padding: 15px;
  border: none;
  border-radius: 10px;
  background: linear-gradient(135deg, var(--orange), var(--orange2));
  color: #fff;
  font-family: 'Orbitron', monospace;
  font-size: 13px;
  font-weight: 700;
  letter-spacing: 2px;
  cursor: pointer;
  transition: transform 0.2s, box-shadow 0.2s, opacity 0.2s;
  box-shadow: 0 6px 25px rgba(255,80,20,0.4);
  display: flex; align-items: center; justify-content: center; gap: 10px;
}
.cta-btn:hover { transform: translateY(-2px); box-shadow: 0 10px 35px rgba(255,80,20,0.5); }
.cta-btn:active { transform: scale(0.98); }
.play-icon {
  width: 0; height: 0;
  border-top: 6px solid transparent;
  border-bottom: 6px solid transparent;
  border-left: 10px solid #fff;
}

/* divider */
.divider {
  display: flex; align-items: center; gap: 12px;
  margin: 1.5rem 0;
  font-size: 11px; letter-spacing: 2px; color: var(--muted);
  text-transform: uppercase;
}
.divider::before, .divider::after {
  content: ''; flex: 1;
  height: 1px; background: var(--border);
}

/* social buttons */
.social-row { display: flex; gap: 12px; }
.social-btn {
  flex: 1; padding: 12px;
  background: var(--input-bg);
  border: 1px solid var(--input-border);
  border-radius: 10px;
  color: var(--text);
  font-family: 'Rajdhani', sans-serif;
  font-size: 14px; font-weight: 600;
  cursor: pointer;
  transition: background 0.2s, border-color 0.2s;
  display: flex; align-items: center; justify-content: center; gap: 8px;
}
.social-btn:hover {
  background: rgba(255,255,255,0.08);
  border-color: rgba(255,255,255,0.25);
}

/* back link */
.back-link {
  margin-top: 1.5rem;
  font-size: 13px; color: var(--muted);
  text-align: center; cursor: pointer;
}
.back-link span { color: var(--cyan); text-decoration: underline; cursor: pointer; }

/* role badge on auth page */
.role-badge {
  display: inline-flex; align-items: center; gap: 6px;
  padding: 4px 14px;
  border-radius: 20px;
  font-size: 11px; font-weight: 700; letter-spacing: 1px;
  text-transform: uppercase;
  margin-bottom: 1.2rem;
}
.role-badge.user-badge  { background: rgba(0,212,220,0.12); color: var(--cyan); border: 1px solid rgba(0,212,220,0.3); }
.role-badge.host-badge  { background: rgba(255,107,53,0.12); color: var(--orange); border: 1px solid rgba(255,107,53,0.3); }

/* toast */
.toast {
  position: fixed; bottom: 110px; left: 50%; transform: translateX(-50%) translateY(20px);
  background: rgba(0,212,220,0.15);
  border: 1px solid var(--cyan);
  color: var(--cyan);
  padding: 10px 24px;
  border-radius: 30px;
  font-size: 13px; font-weight: 600; letter-spacing: 1px;
  opacity: 0;
  transition: opacity 0.4s, transform 0.4s;
  z-index: 999;
  white-space: nowrap;
}
.toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }

@media (max-width: 560px) {
  .role-grid { grid-template-columns: 1fr; max-width: 320px; }
  .auth-card { padding: 1.5rem; }
}
</style>
</head>
<body>

<div class="stars"></div>

<!-- Road + Car -->
<div class="road"></div>
<div class="car-wrap">
  <div class="car">
    <div class="headlight"></div>
    <div class="taillight"></div>
    <div class="car-body">
      <div class="car-windshield"></div>
      <div class="car-stripe"></div>
      <div class="car-badge">P</div>
    </div>
    <div class="wheel wheel-front"></div>
    <div class="wheel wheel-back"></div>
  </div>
</div>

<!-- ═══ PAGE 1: ROLE SELECTION ═══ -->
<div class="page active" id="page-role">
  <div class="logo">
    <span class="p">Park</span><span class="e">E</span>zy
  </div>
  <div class="tagline">Speed &bull; Precision &bull; Parking</div>

  <div class="role-grid">
    <div class="role-card user" onclick="goToAuth('user')">
      <span class="role-icon">🚗</span>
      <div class="role-title">User</div>
      <div class="role-desc">Find & book your perfect parking spot in seconds</div>
      <button class="role-btn">Select</button>
    </div>
    <div class="role-card host" onclick="goToAuth('host')">
      <span class="role-icon">🅿️</span>
      <div class="role-title">Host</div>
      <div class="role-desc">List & manage your parking space, earn revenue</div>
      <button class="role-btn">Select</button>
    </div>
  </div>
</div>

<!-- ═══ PAGE 2: AUTH ═══ -->
<div class="page" id="page-auth">
  <div class="logo">
    <span class="p">Park</span><span class="e">E</span>zy
  </div>
  <div class="tagline">Speed &bull; Precision &bull; Parking</div>

  <div class="auth-card">
    <div id="role-badge-wrap"></div>

    <div class="tab-row">
      <button class="tab active" id="tab-login" onclick="switchTab('login')">Login</button>
      <button class="tab" id="tab-register" onclick="switchTab('register')">Register</button>
    </div>

    <!-- LOGIN FORM -->
    <div id="form-login">
      <div class="field">
        <label>Email / Phone</label>
        <input type="text" placeholder="your@parkezy.in" />
      </div>
      <div class="field">
        <label>Password</label>
        <input type="password" placeholder="••••••••" />
      </div>
      <div class="meta-row">
        <label class="remember">
          <input type="checkbox" /> Remember me
        </label>
        <a href="#" class="forgot">Forgot?</a>
      </div>
      <button class="cta-btn" onclick="handleAuth()">
        <div class="play-icon"></div>
        START PARKING
      </button>
    </div>

    <!-- REGISTER FORM -->
    <div id="form-register" style="display:none;">
      <div class="field">
        <label>Full Name</label>
        <input type="text" placeholder="Your full name" />
      </div>
      <div class="field">
        <label>Email / Phone</label>
        <input type="text" placeholder="your@parkezy.in" />
      </div>
      <div class="field">
        <label>Password</label>
        <input type="password" placeholder="Create a strong password" />
      </div>
      <div class="field">
        <label>Confirm Password</label>
        <input type="password" placeholder="Repeat your password" />
      </div>
      <button class="cta-btn" onclick="handleAuth()">
        <div class="play-icon"></div>
        CREATE ACCOUNT
      </button>
    </div>

    <div class="divider">or continue with</div>
    <div class="social-row">
      <button class="social-btn">
        <svg width="16" height="16" viewBox="0 0 24 24"><path fill="#EA4335" d="M5.266 9.765A7.077 7.077 0 0 1 12 4.909c1.69 0 3.218.6 4.418 1.582L19.91 3C17.782 1.145 15.055 0 12 0 7.27 0 3.198 2.698 1.24 6.65l4.026 3.115Z"/><path fill="#34A853" d="M16.04 18.013c-1.09.703-2.474 1.078-4.04 1.078a7.077 7.077 0 0 1-6.723-4.777l-4.04 3.067A11.965 11.965 0 0 0 12 24c2.933 0 5.735-1.043 7.834-3l-3.793-2.987Z"/><path fill="#4A90E2" d="M19.834 21c2.195-2.048 3.62-5.096 3.62-9 0-.71-.109-1.473-.272-2.182H12v4.637h6.436c-.317 1.559-1.17 2.766-2.395 3.558L19.834 21Z"/><path fill="#FBBC05" d="M5.277 14.268A7.12 7.12 0 0 1 4.909 12c0-.782.125-1.533.357-2.235L1.24 6.65A11.934 11.934 0 0 0 0 12c0 1.92.445 3.73 1.237 5.335l4.04-3.067Z"/></svg>
        Google
      </button>
      <button class="social-btn">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="white"><path d="M12.152 6.896c-.948 0-2.415-1.078-3.96-1.04-2.04.027-3.91 1.183-4.961 3.014-2.117 3.675-.54 9.103 1.519 12.09 1.013 1.454 2.208 3.09 3.792 3.039 1.52-.065 2.09-.987 3.935-.987 1.831 0 2.35.987 3.96.948 1.637-.026 2.676-1.48 3.676-2.948 1.156-1.688 1.636-3.325 1.662-3.415-.039-.013-3.182-1.221-3.22-4.857-.026-3.04 2.48-4.494 2.597-4.559-1.429-2.09-3.623-2.324-4.39-2.376-2-.156-3.675 1.09-4.61 1.09zM15.53 3.83c.843-1.012 1.4-2.427 1.245-3.83-1.207.052-2.662.805-3.532 1.818-.78.896-1.454 2.338-1.273 3.714 1.338.104 2.715-.688 3.559-1.701"/></svg>
        Apple
      </button>
    </div>

    <div class="back-link">
      Wrong role? <span onclick="goBack()">Go back</span>
    </div>
  </div>
</div>

<!-- Toast -->
<div class="toast" id="toast"></div>

<script>
var currentRole = 'user';

function goToAuth(role) {
  currentRole = role;
  localStorage.setItem('role', role);

  var badge = document.getElementById('role-badge-wrap');
  if (role === 'user') {
    badge.innerHTML = '<div class="role-badge user-badge">🚗 &nbsp;User Mode</div>';
  } else {
    badge.innerHTML = '<div class="role-badge host-badge">🅿️ &nbsp;Host Mode</div>';
  }

  document.getElementById('page-role').classList.remove('active');
  document.getElementById('page-auth').classList.add('active');
  switchTab('login');
  window.scrollTo(0, 0);
}

function goBack() {
  document.getElementById('page-auth').classList.remove('active');
  document.getElementById('page-role').classList.add('active');
  window.scrollTo(0, 0);
}

function switchTab(tab) {
  document.getElementById('tab-login').classList.toggle('active', tab === 'login');
  document.getElementById('tab-register').classList.toggle('active', tab === 'register');
  document.getElementById('form-login').style.display    = tab === 'login'    ? 'block' : 'none';
  document.getElementById('form-register').style.display = tab === 'register' ? 'block' : 'none';

  var btn = document.querySelector('#form-' + tab + ' .cta-btn');
  if (btn) {
    btn.innerHTML = tab === 'login'
      ? '<div class="play-icon"></div> START PARKING'
      : '<div class="play-icon"></div> CREATE ACCOUNT';
  }
}

function handleAuth() {
  var isLogin = document.getElementById('form-login').style.display !== 'none';
  var msg = isLogin
    ? (currentRole === 'user' ? 'Welcome back! Redirecting to dashboard…' : 'Host dashboard loading…')
    : 'Account created! Welcome to ParkEzy!';
  showToast(msg);
}

function showToast(msg) {
  var t = document.getElementById('toast');
  t.textContent = msg;
  t.classList.add('show');
  setTimeout(function(){ t.classList.remove('show'); }, 3000);
}
</script>
</body>
</html>